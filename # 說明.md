# 主要日誌

一開始測試的時候是用yolo給的育訓練模型來跑的，輸出之後有很多問題，主要是有輸出很多不必要的物件，輸出為person, chair, cellphone, bottle 這幾個偵測到的東西，但是人物沒有完整偵測到，且yolo不知道"公車座位"是甚麼，因此是輸出椅背，加上被擋住的關係，導致座位沒有正確被判斷。

綜合以上，我的直覺告訴我:
照直覺來看，我們盡量事先設計好這些事情 
- 穩定偵測person 
- 自己畫seat bbox 
- 讓yolo學習seat :
- 過濾不必要的class(例如水壺或其他物件之類的，做到只偵測person和seat) 
最後想辦法在用之前的json格式來讓做特定方式或函數的輸出 我可能也會比較想用這種進度做，不然我自己的直覺告訴我這樣下去會很危險。

但也是因為這樣，之後雖然花了三天基本上是沒有休息的努力完成模型訓練與校正流程方面，但也因此提早解決了後續可能反覆遇到的座位辨識、ID排序及JSON輸出一致性問題。
途中有遇到一些事情，包含
- 畫bbox的時候發生無法匯出或程式崩潰(之後確定是程式與目前python版本不相容，因此換了另一個工具)
- 訓練後測試出現很多錯誤偵測到的座位等等，詳細更改可以看以下
---

另外在這裡先說一些東西:
首先是:(資料來源與版權說明)

### 攝影機測試
由於公車內部影像涉及乘客隱私，且實際拍攝與公開使用需取得相關授權，因此本專題在攝影機人物偵測測試時，原則上以拍攝電腦螢幕顯示結果的方式進行驗證，避免直接公開乘客影像。
若未來需要於實際環境進行測試，將遵守相關法規與授權規範。

### 訓練資料來源
本專題使用之訓練圖片可能包含：
- 自行製作資料
- AI 生成圖片(人物方面為降低涉及個人隱私的風險，部分人物圖片採用 AI 生成方式製作。AI 生成圖片可能與實際拍攝影像存在差異，因此主要作為模型開發與初步驗證用途。)
- 具有公開授權（例如 CC-BY-4.0）的圖片(具體圖片的連結都在"assets/training_files/Picture_Source.csv")

使用公開授權圖片時，將依照其授權條款使用，並於公開發布成果時補充必要的版權與來源資訊。

### 專題公開說明
目前使用之資料主要作為模型訓練、測試及研究用途。
若未來公開資料集、模型或程式碼，將依各資料來源之授權條款提供適當的版權聲明、引用來源及授權資訊。
---

接下來是輸出的主要格式:
(此格式目前預設為後續交接資料所使用的統一格式)

```json

{
  "occupied_seats": {
    "A01": "empty",
    "A02": "empty",
    "A03": "empty",
    "A04": "empty",
    "A05": "empty",
    "A06": "empty",
    "A07": "empty",
    "A08": "empty",
    "B01": "empty",
    "B02": "empty",
    "B03": "empty",
    "B04": "empty",
    "B05": "empty",
    "B06": "occupied",
    "B07": "empty",
    "B08": "empty"
  },
  "occupied_count": 1,
  "total_seats": 16,
  "person_count": 1
}

```
輸出的部分主要是看座位是否被占用，我有把每個座位透過ID校正，在經歷很多次嘗試之後有成功做好排序
考慮到UI上面是`4*2, 4*2`的總類的，因此主要排序為:
A07 A08    B07 B08
A05 A06    B05 B06
A03 A04    B03 B04
A01 A02    B01 B02

另外empty是指空座位(沒被占用)
        occupied則是指被占用

occupied_count是被占用的統計
total_seats是主要座位數(有一個新增的程式有寫預設值和輸出座位的座標，
大於或小於座位預設值會顯示座位數量或座位輸出後的排序及座標可能有問題，就會變成需要再重新校正)
person_count是指人數統計
---
# 主要運作與測試流程(這是相對來說大家會需要知道的，裡面我大概只說流程就好，不會講帶多麻煩的地方)

大家如果要測試的話，直接執行run_inference.py應該就可以了，我跑的指令為
```
python run_inference.py --image assets/pictures/main/main2.png --model runs/detect/seat_detector-3/weights/best.pt --calibration seat_calibration.json
```

具體自己的流程是這樣的:

1. 先執行一次 assets/training_files/test.py (目的為測試偵測方面)
2. 執行run_calibration.py，這是用於執行calibrate.py的CLI程式碼，主要用於座位ID校正，他會在執行後輸出calibration_preview.png，用於確認座位ID是否符合期待
   並且，這會在確認後生成seat_calibration.json，但這目前是座位ID的類似座標，用於檢查之後的"相關"圖片，若是不相關的，要再從新執行一次ID校正。
      此外，這是我執行的指令:
      ```
      python run_calibration.py --image assets/pictures/main/main2.png --model runs/detect/seat_detector-3/weights/best.pt --seats-per-side 2 --conf 0.5 --dedup-iou 0.85 --row-order near_first
      ```
3. 執行run_inference.py 這是用於輸出主要json格式的程式碼，可以符合上方格式輸出內容，這也是一個CLI，方便調整參數
   具體我執行的程式為:
   ```
   python run_inference.py --image assets/pictures/main/main2.png --model runs/detect/seat_detector-3/weights/best.pt --calibration seat_calibration.json
   ```
   另外，加上 --output "路徑" 就可以輸出json，但我為了測試是先讓他印在終端機，所以沒做output
---

# 訓練好的模型位置

在 runs/detect/seat_detector-3/weights/best.pt 這個位置，我在先前提到的: "assets/training_files/test.py"裡面有先寫好這邊了。

## 訓練模型的檔案

在 seat_dataset/ 這個目錄底下，是我主要用於順練與考模型的檔案，另外 assets/training_files/ 這目錄底下主要是我放一些資料的地方，裡面有yolo用於判斷位置的txt，預設的json(用於其他種類的模型做深度學習，我們yolo模型主要是用txt的檔案)，另外在此目錄下，應該有看到一個叫做 Picture_Source.csv的檔案，這是用於放一些圖片來源的地方，為了避免版權問題而做的

此外，assets/pictures/main 是我主要用於最終測試的圖片，其中其他三個都能正確輸出座位占用狀況，另外 main.png 的輸出，輸出的座位與人數佔用狀況皆為0是正常的，這是空的座位圖，用於座位ID校正的關鍵圖片，這用於最終測試之後輸出的也確實是正常的全部為空座位

對了，log.log的這個檔案是我跑一些指令產生的結果，用於讓我和大家知道一些問題或測試狀況用的
---

# 這些是我在這幾天梭做的變更(具體來說是7/14~7/17今天，對程式方面的部分，此外，此說明是在7/17做的)

## 1. 2026-07-14 ｜commit id: e8a0d69｜加入訓練檔案與順練模型
- 內容：加入座位偵測訓練所需的訓練資料、標註檔與資料設定檔，建立後續訓練與測試的基礎資料。
- 變更的檔案為：
  - .gitignore, 26.7.6/v8_test.py, seat_dataset/data.yaml, seat_dataset/images/train, seat_dataset/images/val, seat_dataset/labels/train, seat_dataset/labels/val

## 2. 2026-07-15｜commit id: c273186｜檢查首次 ID 校準，未測試，只有先檢查程式碼
- 內容：先針對 ID 校準相關程式做程式碼檢查與初步確認，尚未進行實際測試。
- 變更的檔案為：
  - calibrate.py

## 3. 2026-07-15｜commit id: 18d6665｜第一次做 ID 校正
- 內容：初次進行 ID 校正流程的實作，嘗試讓座位 ID 能正確顯示；但在實際情況下，仍有部分 ID 重疊或未正確顯示的問題。
- 變更的檔案為：
  - run_calibration.py

## 4. 2026-07-15｜commit id: b365c79｜修改 ID 校正邏輯
- 內容：針對 ID 校正流程進一步調整，修正座位 ID 的判定與顯示邏輯。
- 變更的檔案為：
  - calibrate.py

## 5. 2026-07-15｜commit id: 2f9c2d7｜第二次 ID 校正
- 內容：再次調整 ID 校正，修正重疊框被誤判為需要標記 ID 的情況。
- 變更的檔案為：
  - calibrate.py

## 6. 2026-07-15｜commit id: bfe1fc6｜修正 ID 校正問題，並補上漏掉的訓練資料
- 內容：修正 ID 校正相關問題，同時補上先前未納入的訓練圖片、標註檔與預設位置資料。
- 變更的檔案為：
  - .gitignore, calibrate.py, run_calibration.py, seat_dataset/images/train, seat_dataset/images/val, seat_dataset/labels/train, seat_dataset/labels/val, 預設位置.txt

## 7. 2026-07-15｜commit id: 2c01325｜修正 ID 校正位置與排序方式
- 內容：調整 ID 校正的定位方式與 ID 排序邏輯，使輸出結果更穩定。
- 變更的檔案為：
  - calibrate.py, run_calibration.py

## 8. 2026-07-15｜commit id: 9cd1102｜新增 JSON 輸出格式
- 內容：新增輸出的 JSON 方式與格式，讓結果可更完整地記錄座位與人數相關資訊；當時仍有已知錯誤。
- 變更的檔案為：
  - run_calibration.py, run_inference.py, seat_calibration.json

## 9. 2026-07-16｜commit id: 9876d00｜修正人數與座位占用數量不符
- 內容：針對實際人數與座位占用數量不一致的情況進行修正。
- 變更的檔案為：
  - run_inference.py

## 10. 2026-07-16｜commit id: 6c85620｜完成人數與占用座位數量對齊修正
- 內容：完成人數與座位占用數量對齊的修正，確認輸出的人數與占用座位數量一致，並以 main2 圖進行初步驗證。
- 變更的檔案為：
  - run_inference.py

## 11. 2026-07-16｜commit id: abb05fc｜確認圖片基準資料
- 內容：確認 main4（原始 MainTest.jpg）與其他圖片屬於不同角度與視角，因此後續以 main.png、main2.png、main3.png 作為主要基準。
- 變更的檔案為：
  - run_inference.py
---

## 小結
- 本段開發重點主要集中在：
  1. 訓練資料與標註檔的建立與補齊。
  2. ID 校準流程的逐步調整與修正。
  3. 輸出 JSON 格式與推論結果的完善。
  4. 實際人數與座位占用數量一致性的修正。
---
